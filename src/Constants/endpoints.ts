const PREFIX = "api/v1";


export default {
    Auth: {
        SignIn: `${PREFIX}/auth/login`,
        SignOut: ``
    },
    Advert: {
        Edit: `${PREFIX}/`,
        Join: `${PREFIX}/advert/join/`
    }
}